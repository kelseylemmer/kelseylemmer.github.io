// var header = document.querySelectorAll('.header')[0];

// header.style.color = '#f00';

// get all details elements from inside work experience card (add an id to the card containing all of my work experience)
// loop through all of those elements and apply the "open" html attribute to each of them

var x = document.getElementById("workExperience").open;

function myFunction() {
    document.getElementById("workExperience").open = true;
}
